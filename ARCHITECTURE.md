# 项目架构文档：DQN-Game2048

## 项目概述

这是一个基于 **深度 Q 网络（Deep Q-Network, DQN）** 的强化学习项目，目标是训练 AI 自动玩 2048 游戏。项目使用 PyTorch 框架，Python 3.13。

---

## 项目结构

```
DQN-game2048/
├── config.py              # 超参数与路径配置
├── game_env.py            # 2048 游戏环境（棋盘逻辑）
├── dqn_agent.py           # DQN 网络结构 + Agent + 优先级经验回放
├── train.py               # 训练主程序 + 奖励函数
├── test.py                # 模型测试 + 手动输入棋盘推理
├── gui.py                 # Tkinter 图形界面可视化
├── chart/                 # 图表生成模块（matplotlib）
│   ├── __init__.py        # 图表入口、日志解析、图表分发
│   ├── histogram.py       # Max Tile 柱状图
│   ├── frequency.py       # Score 频数分布直方图
│   ├── pie.py             # Max Tile 饼图
│   └── scatter.py         # 散点图（Score/Max Tile/Steps）
├── final_modal/           # 训练完成的最终模型（.pth）
├── temp_model/            # 训练过程中的检查点（每100轮保存）
├── log/                   # 训练/测试日志
├── chart/pic/             # 生成的统计图表
└── result/                # 运行截图和录屏
```

---

## 核心模块详解

### 1. 游戏环境 (`game_env.py`)

**类：`Game2048`**

实现完整的 2048 游戏逻辑：

- **棋盘**：`numpy` 4x4 整型数组
- **状态表示**：`np.log2(grid + 1e-5)` 取对数后 reshape 为 `(4, 4, 1)`，供 CNN 输入
- **方向移动**：通过 `np.rot90` 旋转矩阵，将四个方向的移动统一为 `_move_left` 处理，代码简洁优雅
- **核心合并算法**：`_move(row)` 方法——去零、相邻合并、补齐零
- **新方块生成**：90% 概率生成 2，10% 概率生成 4
- **有效动作检测**：`get_valid_actions()` 模拟执行每个方向，返回实际能改变棋盘的动作索引
- **游戏结束判定**：`game_over()` 检查是否存在任意有效动作

---

### 2. DQN 智能体 (`dqn_agent.py`)

#### 网络结构（CNN）

```
输入： (batch, 1, 4, 4)
    ↓
Conv2d(1, 32, kernel=3, padding=1) + ReLU    ← 保持 4x4 尺寸
    ↓
Conv2d(32, 64, kernel=3, padding=1) + ReLU
    ↓
Conv2d(64, 64, kernel=3, padding=1) + ReLU
    ↓
Flatten → 1024 维向量
    ↓
Linear(1024, 512) + ReLU
    ↓
Linear(512, 4)  ← 输出 4 个动作的 Q 值
```

#### 关键技术组件

| 技术 | 说明 |
|------|------|
| **双重 DQN** | `model` 用于选择动作，`target_model` 用于计算 Q 目标，定期同步参数 |
| **优先级经验回放 (PER)** | `PrioritizedReplayBuffer` 按 TD 误差越大越优先的原则采样经验 |
| **重要性采样权重** | 校正优先级采样引入的分布偏差，`beta` 从 0.4 逐步增长到 1.0 |
| **有效动作屏蔽** | `act()` 只在当前局面的有效动作中选择，避免浪费步数 |
| **epsilon 线性衰减** | 探索率从 1.0 线性衰减到 0.05（前 90% 训练周期） |

---

### 3. 奖励函数设计 (`train.py`)

奖励由 **四个分量** 加权组合：

| 分量 | 公式 | 目的 |
|------|------|------|
| **分数奖励** | `env.score - prev_score` | 鼓励合并方块获取即时得分 |
| **最大方块奖励** | `max_tile × (1 + 0.1 × log₂(max_tile))` | 强化合成大数字（指数级增长） |
| **空位奖励** | `empty² - prev_empty²` | 鼓励保留更多空位，避免棋盘过早填满 |
| **单调性奖励** | `monotonicity × 5` | 鼓励数值按行/列有序排列（蛇形策略基础） |

单调性计算：遍历 4 行 + 4 列，统计相邻元素递减的次数，满分 24。

---

### 4. 训练流程 (`train.py`)

```
输入 episode 数
    ↓
初始化 DQNAgent + 日志
    ↓
for each episode:
    ├── 重置环境 Game2048()
    ├── while not game_over:
    │   ├── 获取有效动作
    │   ├── agent.act() 选择动作（epsilon-greedy）
    │   ├── env.move() 执行动作
    │   ├── calculate_reward() 计算奖励
    │   ├── agent.remember() 存储经验
    │   └── agent.replay() 经验回放训练
    ├── epsilon 线性衰减
    ├── 每 20 轮更新目标网络
    └── 每 100 轮保存 checkpoint
    ↓
保存最终模型 → 可选生成图表 → 可选自动测试
```

---

### 5. 测试模块 (`test.py`)

两种模式：

- **批量测试**：`test_model()` 加载模型运行 N 次完整游戏，记录得分/最大方块/步数到日志
- **手动推理**：用户输入 4x4 棋盘，模型输出推荐动作（用于单局面诊断）

---

### 6. GUI 可视化 (`gui.py`)

使用 **Tkinter** 实现实时游戏展示：

- 4x4 彩色数字方块，数字越大颜色越深
- 实时显示分数和步数
- 支持选择训练检查点或最终模型
- 可调节刷新速率

---

### 7. 图表模块 (`chart/`)

基于 **matplotlib** 的日志数据分析与可视化：

| 图表类型 | 训练集 | 测试集 | 说明 |
|----------|--------|--------|------|
| 散点图 | Score / Max Tile / Steps | — | 按 episode 顺序展示趋势 |
| 柱状图 | Max Tile 分布 | Max Tile 分布 | 各数字的出现频率 |
| 频数直方图 | Score 分布 | Score 分布 | 得分区间频数 + 均值/最大值 |
| 饼图 | — | Max Tile 比例 | 测试中各数字占比 |

---

## 配置参数 (`config.py`)

| 参数 | 值 | 说明 |
|------|-----|------|
| `update_target_freq` | 20 | 目标网络更新间隔（episode） |
| `maxlen` | 10000 | 经验回放缓冲区容量 |
| `replay_beta` | 0.4 | PER 重要性采样初始 β |
| `gamma` | 0.99 | 折扣因子 |
| `epsilon` | 1.0 | 初始探索率 |
| `epsilon_min` | 0.05 | 最小探索率 |
| `learning_rate` | 0.0001 | Adam 学习率 |
| `batch_size` | 128 | 每次训练采样的经验数 |

---

## 开发迭代历程

### 初版
- FC 全连接网络，16 维展平状态
- `reward = score_diff` 单一奖励
- epsilon 指数衰减到 0.01
- **问题**：模型无法学习，有效动作检测缺失导致大量无效步数

### 第一次修改
- 改用 CNN 保留空间结构
- `log2` 状态表示压缩数值范围
- 有效动作过滤避免无效步数
- 复合奖励函数（得分 + 最大方块 + 空位 + 步数惩罚）
- epsilon 改为线性衰减，下限提升到 0.05
- 加入 tqdm 进度条和 logging 日志
- **效果**：模型开始展现学习能力，但训练效果不稳定

### 第二次修改
- 优化奖励函数：最大方块奖励加入指数系数，空位奖励改用平方差，新增单调性奖励
- **效果**：1000 次训练后显著进步，256 占比从 3% 提升到 31%，512 占比 8%
- **问题**：延长到 5000 次训练后表现反而退化

### 第三次修改（当前版本）
- 引入优先级经验回放（PER），按 TD 误差优先采样高价值经验
- 加入重要性采样权重，逐步从 0.4 增长到 1.0
- 超参数集中到 `config.py` 统一管理
- batch_size 从 32 增大到 128
- 目标网络更新频率从 50 提升到 20
- **效果**：学习能力显著增强，1024 首次出现（2%），512 占比达 18%

---

## 待改进方向

1. **探索策略**：当前线性衰减在 90% 周期后 epsilon 恒定为 0.05，后期缺乏微调能力
2. **目标网络更新**：可考虑改用软更新（soft update）替代硬拷贝，使训练更平滑
3. **网络结构**：可尝试增大 CNN 感受野（如 5x5 卷积核）或引入注意力机制
4. **长周期训练退化**：5000 次以上训练时的性能退化问题尚未完全解决，可能与经验回放缓冲区早期低质量经验污染有关
5. **混合架构**：可探索 DQN + MCTS（蒙特卡洛树搜索）的混合方案，提升长期规划能力
